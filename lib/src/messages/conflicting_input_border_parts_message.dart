const conflictingInputBorderPartsMessage =
    'Conflicting input border parts: a border slot cannot resolve as both '
    'OutlineInputBorder, UnderlineInputBorder, and InputBorder.none. Use only '
    'one input border part family per border slot.';

const inputBorderSidePartsRequireFrameMessage =
    'Input border side parts require an existing input border or a concrete '
    'input border family part.';
